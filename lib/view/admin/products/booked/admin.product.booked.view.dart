import 'package:flutter/material.dart';

class BookedProduct extends StatelessWidget {
  const BookedProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
